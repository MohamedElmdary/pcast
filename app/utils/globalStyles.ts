import { StyleSheet } from 'react-native';

const GlobalStyles = StyleSheet.create({
  flexRowCenter: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
});

export { GlobalStyles };
